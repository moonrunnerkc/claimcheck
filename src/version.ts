/**
 * The tool version, recorded in every evidence record so a bundle hash is tied
 * to the exact detection logic that produced it. Bump this when a detection
 * change should invalidate previously cached or replayed verdicts.
 */
export const TOOL_VERSION = "0.1.0";
